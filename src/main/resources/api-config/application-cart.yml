# 购物车模块配置

api:
  routes:
    - path: /api/cart/add
      procedure: add_to_cart
      method: POST
    - path: /api/cart/user/{userId}
      procedure: get_user_cart
      method: GET

procedures:
  add_to_cart:
    sql: "CALL sp_add_to_cart(?, ?, ?)"
  get_user_cart:
    sql: "SELECT * FROM fn_get_user_cart(?)"
